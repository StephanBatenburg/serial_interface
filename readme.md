# Serial interface

A program to connect to any serial communication device.

> [!IMPORTANT]
> This projects is still under development!
> No garantees that anything works yet!

## How to build
Build this projects using CMake and vcpkg. After you installed both of those and defined VCPKG_ROOT in your system variables

```shell
cmake --preset=default
cmake --build build
```